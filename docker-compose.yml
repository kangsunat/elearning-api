version: "3.8"

services:
  mongodb:
    image: mongo:latest
    container_name: elearning-api
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: kangsunatdev_db_user
      MONGO_INITDB_ROOT_PASSWORD: OkGD5tcymAhYNIFf
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
